[project]
name = "apt-analyzer"
version = "0.1.0"
description = "Analyze apartment transaction liquidity and price resilience."
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "fastapi>=0.141.1",
    "jinja2>=3.1.6",
    "python-multipart>=0.0.32",
    "uvicorn>=0.52.4",
]

[project.scripts]
apt-analyzer = "apt_analyzer.cli:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "commitizen>=4.18.0",
    "djlint>=1.44.2",
    "httpx2>=2.12.0",
    "pre-commit>=4.6.2",
    "pre-commit-hooks>=6.0.0",
    "pyright>=1.1.411",
    "pytest>=9.1.1",
    "pytest-playwright>=0.9.0",
    "ruff>=0.16.4",
]

[tool.ruff]
target-version = "py314"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B", "UP", "D"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.per-file-ignores]
"scripts/**/*.py" = ["D"]
"tests/**/*.py" = ["D"]

[tool.ruff.format]
docstring-code-format = true
line-ending = "lf"

[tool.pyright]
include = ["src"]
pythonVersion = "3.14"
typeCheckingMode = "strict"

[tool.pytest.ini_options]
minversion = "8.0"
addopts = ["--strict-config", "--strict-markers", "-ra"]
testpaths = ["tests"]
markers = [
    "e2e: browser-backed deterministic tests (opt-in)",
    "live: tests that call external data sources (opt-in)",
]

[tool.commitizen]
name = "cz_conventional_commits"
version = "0.1.0"
version_provider = "pep621"
tag_format = "v$version"
